# React Props – Parent to Child & Child to Parent Communication

This project is a simple React practice app that demonstrates how **props** work in React, including:

- Passing data from **Parent → Child**
- Sending data/events from **Child → Parent** using functions

This repository was created for learning and practice purposes.

---

## 🚀 Concepts Covered

### 1️⃣ Parent to Child Communication
- Data is passed from a parent component to a child component using **props**
- The child receives and uses the data without modifying it

### 2️⃣ Child to Parent Communication
- A function is passed from the parent to the child as a prop
- The child calls that function to send data back to the parent

---

## 🛠️ Tech Stack

- React (Vite)
- JavaScript (ES6)
- JSX
- CSS (optional styling)



